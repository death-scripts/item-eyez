<UserControl
    x:Class="item_eyez.main_view"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:item_eyez"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="550"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <LinearGradientBrush x:Key="TabBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="Purple" Offset="0" />
            <GradientStop Color="Orange" Offset="0.25" />
            <GradientStop Color="Red" Offset="0.5" />
            <GradientStop Color="Green" Offset="0.75" />
            <GradientStop Color="Yellow" Offset="1" />
        </LinearGradientBrush>
    </UserControl.Resources>
    <Grid>
        <TabControl Background="{StaticResource TabBackgroundBrush}">
            <TabItem Header="ROOMS">
                <local:rooms_view />
            </TabItem>
            <TabItem Header="CONTAINERS">
                <local:containers_view />
            </TabItem>
            <TabItem Header="ITEMS">
                <local:items_view />
            </TabItem>
            <TabItem Header="ORGANIZE">
                <local:organize_view />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
